@extends('admin.layout') @section('title',$customer->name) @section('content')<div class="page-head"><div><div class="eyebrow">Customer</div><h1>{{ $customer->name }}</h1><p>{{ $customer->phone }} · {{ $customer->email }}</p></div><a class="btn" href="{{ route('admin.customers.edit',$customer) }}">Edit</a></div><div class="card"><strong>Address</strong><p>{{ $customer->address ?: 'Not provided' }}</p></div><h2>Online Orders</h2><div class="table-wrap"><table><thead><tr><th>Order</th><th>Date</th><th>Status</th><th>Total</th></tr></thead><tbody>@forelse($customer->orders as $order)<tr><td><a href="{{ route('admin.orders.show',$order) }}">{{ $order->order_no }}</a></td><td>{{ $order->created_at->format('d M Y') }}</td><td>{{ ucfirst($order->status) }}</td><td>{{ number_format($order->grand_total,2) }}</td></tr>@empty<tr><td colspan="4">No online orders.</td></tr>@endforelse</tbody></table></div><h2>POS Sales</h2><div class="table-wrap"><table><thead><tr><th>Invoice</th><th>Date</th><th>Total</th></tr></thead><tbody>@forelse($customer->sales as $sale)<tr><td><a href="{{ route('admin.sales.show',$sale) }}">{{ $sale->invoice_no }}</a></td><td>{{ $sale->created_at->format('d M Y') }}</td><td>{{ number_format($sale->grand_total,2) }}</td></tr>@empty<tr><td colspan="3">No POS sales.</td></tr>@endforelse</tbody></table></div>@endsection
