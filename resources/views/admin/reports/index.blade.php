@extends('admin.layout') @section('title','Reports') @section('content')<div class="page-head"><div><div class="eyebrow">Analytics</div><h1>Reports</h1><p>{{ $from->format('d M Y') }} to {{ $to->format('d M Y') }}</p></div></div><form class="card search-row" method="GET"><select name="period"><option value="daily" @selected($period==='daily')>Daily</option><option value="weekly" @selected($period==='weekly')>Weekly</option><option value="monthly" @selected($period==='monthly')>Monthly</option><option value="custom" @selected($period==='custom')>Custom</option></select><input type="date" name="from" value="{{ request('from') }}"><input type="date" name="to" value="{{ request('to') }}"><button class="btn">Apply</button></form><div class="grid" style="margin-top:16px"><div class="card"><div class="stat-label">Sales</div><div class="stat">{{ $salesCount }}</div></div><div class="card"><div class="stat-label">Revenue</div><div class="stat">{{ number_format($revenue,2) }}</div></div><div class="card"><div class="stat-label">Gross Profit</div><div class="stat">{{ number_format($profit,2) }}</div></div><div class="card"><div class="stat-label">Inventory Value</div><div class="stat">{{ number_format($inventoryValue,2) }}</div></div></div><div class="form-grid" style="margin-top:16px"><div class="card"><h2>Top Products</h2>@foreach($topProducts as $item)<div class="summary-row"><span>{{ $item->product_name }}</span><strong>{{ number_format($item->quantity,3) }}</strong></div>@endforeach</div><div class="card"><h2>Stock Health</h2><div class="stat">{{ $lowStock }}</div><p>products at or below low-stock level</p><a class="btn" href="{{ route('admin.inventory.low-stock') }}">View Low Stock</a></div></div>@endsection
