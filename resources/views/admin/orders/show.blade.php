@extends('admin.layout') @section('title',$order->order_no) @section('content')<div class="page-head"><div><div class="eyebrow">Order</div><h1>{{ $order->order_no }}</h1><p>{{ $order->customer_name }} · {{ $order->customer_phone }}</p></div><form method="POST" action="{{ route('admin.orders.status',$order) }}">@csrf @method('PATCH')<div class="stack"><select name="status">@foreach(['pending','processing','delivered','cancelled'] as $status)<option value="{{ $status }}" @selected($order->status===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="btn">Update Status</button></div></form></div><div class="card"><p><strong>Shipping:</strong> {{ $order->shipping_address }}</p><p><strong>Payment:</strong> {{ ucfirst(str_replace('_',' ',$order->payment_method)) }}</p></div><div class="table-wrap" style="margin-top:16px"><table><thead><tr><th>Product</th><th>SKU</th><th>Qty</th><th>Price</th><th>Total</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }} {{ $item->variant_name }}</td><td>{{ $item->sku }}</td><td>{{ $item->quantity }}</td><td>{{ number_format($item->unit_price,2) }}</td><td>{{ number_format($item->line_total,2) }}</td></tr>@endforeach</tbody></table></div><div class="card" style="margin-top:16px;max-width:440px;margin-left:auto"><div class="summary-row"><span>Subtotal</span><strong>{{ number_format($order->subtotal,2) }}</strong></div><div class="summary-row"><span>Discount</span><strong>{{ number_format($order->discount_total,2) }}</strong></div><div class="summary-row"><span>Tax</span><strong>{{ number_format($order->tax_total,2) }}</strong></div><div class="summary-row total"><span>Total</span><strong>{{ number_format($order->grand_total,2) }}</strong></div></div>@endsection
