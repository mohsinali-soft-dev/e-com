@extends('admin.layout') @section('title','Orders') @section('content')<div class="page-head"><div><div class="eyebrow">Online Store</div><h1>Orders</h1></div></div><form class="card search-row" method="GET"><input name="search" value="{{ request('search') }}" placeholder="Order, customer or phone"><select name="status" style="max-width:190px"><option value="">All statuses</option>@foreach(['pending','processing','delivered','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="btn">Filter</button></form><div class="table-wrap" style="margin-top:16px"><table><thead><tr><th>Order</th><th>Customer</th><th>Date</th><th>Items</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->order_no }}</strong></td><td>{{ $order->customer_name }}<br>{{ $order->customer_phone }}</td><td>{{ $order->created_at->format('d M Y') }}</td><td>{{ $order->items_count }}</td><td>{{ number_format($order->grand_total,2) }}</td><td><span class="badge">{{ ucfirst($order->status) }}</span></td><td><a class="btn btn-light" href="{{ route('admin.orders.show',$order) }}">Details</a></td></tr>@empty<tr><td colspan="7">No orders.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}@endsection
