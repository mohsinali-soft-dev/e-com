<article class="card">@if($product->image_path)<img class="product-img" src="{{ asset('storage/'.$product->image_path) }}" alt="{{ $product->name }}">@else<div class="product-img" style="display:grid;place-items:center;color:#64748b">No image</div>@endif
<p style="color:#64748b;margin-bottom:4px">{{ $product->category?->name }}</p><h3 style="margin:0 0 10px">{{ $product->name }}</h3><div class="price">{{ \App\Models\Setting::current()->currency }} {{ number_format($product->has_variants ? $product->variants->min('selling_price') : $product->selling_price,2) }}</div><a class="btn" style="margin-top:14px" href="{{ route('shop.show',$product) }}">View Product</a></article>
