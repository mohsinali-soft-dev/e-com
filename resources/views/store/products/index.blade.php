@extends('store.layout') @section('title','Shop') @section('content')
<section class="section"><div class="container"><h1 style="font-size:40px">Shop</h1><form class="card form-grid" method="GET"><input name="search" value="{{ request('search') }}" placeholder="Search products"><select name="category"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category')===$category->slug)>{{ $category->name }}</option>@endforeach</select><button class="btn">Search</button></form><div class="grid" style="margin-top:22px">@forelse($products as $product)@include('store.products.card')@empty<div class="card">No products found.</div>@endforelse</div><div style="margin-top:20px">{{ $products->links() }}</div></div></section>
@endsection
