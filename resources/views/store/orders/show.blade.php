@extends('store.layout') @section('title',$order->order_no) @section('content')<section class="section"><div class="container"><h1 style="font-size:42px">{{ $order->order_no }}</h1><div class="card"><p>Status: <strong>{{ ucfirst($order->status) }}</strong></p><p>Delivery: {{ $order->shipping_address }}</p>@foreach($order->items as $item)<div style="display:flex;justify-content:space-between;padding:10px 0;border-bottom:1px solid #e2e8f0"><span>{{ $item->product_name }} {{ $item->variant_name }} × {{ $item->quantity }}</span><strong>{{ \App\Models\Setting::current()->currency }} {{ number_format($item->line_total,2) }}</strong></div>@endforeach<h2>Total: {{ \App\Models\Setting::current()->currency }} {{ number_format($order->grand_total,2) }}</h2></div></div></section>@endsection
