@extends('store.layout') @section('title','My Orders') @section('content')<section class="section"><div class="container"><h1 style="font-size:42px">My Orders</h1><form class="card" method="GET"><label>Phone number used at checkout</label><div style="display:flex;gap:10px"><input name="phone" value="{{ request('phone') }}" required><button class="btn">Find Orders</button></div></form><div class="grid" style="margin-top:18px">@foreach($orders as $order)<a class="card" href="{{ route('orders.track.show',['order'=>$order,'phone'=>request('phone')]) }}"><strong>{{ $order->order_no }}</strong><p>{{ $order->created_at->format('d M Y') }} · {{ ucfirst($order->status) }}</p><div class="price">{{ \App\Models\Setting::current()->currency }} {{ number_format($order->grand_total,2) }}</div></a>@endforeach</div></div></section>@endsection
